# Use legacy peer-deps resolution, matching local dev and CI
# (all npm commands in .github/workflows use --legacy-peer-deps).
# Without this, EAS cloud builds run plain `npm ci` and fail with
# "package.json and package-lock.json are in sync" EUSAGE errors.
legacy-peer-deps=true
